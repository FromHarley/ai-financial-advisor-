# Core app
streamlit>=1.39.0
python-dotenv>=1.0.1

# Layer 1 — ML + SHAP
xgboost>=2.0.3
scikit-learn>=1.5.2
pandas>=2.2.3
numpy>=1.26.4
shap>=0.46.0
matplotlib>=3.9.2
joblib>=1.4.2

# Layer 2 — GenAI (Claude)
anthropic>=0.39.0

# Layer 3 — Agentic (market data)
yfinance>=0.2.48

# Voice / UX
streamlit-mic-recorder>=0.0.8
openai>=1.54.3
